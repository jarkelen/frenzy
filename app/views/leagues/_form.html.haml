= simple_form_for @league, html: { class: 'form-horizontal' } do |f|
  = f.input :league_name
  = f.input :league_short
  = f.input :level
  .form-actions
    = f.button :submit, class: 'btn'
    = link_to t('.cancel', default: t("helpers.links.cancel")), leagues_path, class: 'btn'
